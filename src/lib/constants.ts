import type { Preferences } from "./types"

const defaultPreferences: Preferences = {
    background: "#ccc",
    stroke: {
        color: "black",
        width: 1
    }
}


export const CONSTANTS = {
    defaultPreferences
}
